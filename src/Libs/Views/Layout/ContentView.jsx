import styled from "styled-components";

const ContentView = styled.div`
    position: relative;
    flex: 1;
    overflow-x: hidden;
    overflow-y: auto;
    background: #f1f1f1;
`;

export default ContentView;
