import styled from "styled-components";

const width = '0.44rem;'

const BjView = styled.div`
   text-align: center;
   line-height: ${width};
   height: ${width};
   min-height: ${width};
   position: relative;
   bottom: 0;
   left: 0;
   width: 100%;
   background: #fff;
   font-size: 0.16rem;
   color: #108ee9;
`;

export default BjView;